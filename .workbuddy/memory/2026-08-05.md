# 2026-08-05

- 参照上游 `chenyme/grok2api` 重构 Console SSO：使用 SSO Cookie 向 `/v1/dpop/token` 交换绑定 P-256 JWK 的短期 token，并为 Console `/v1/*` 请求生成 ES256 DPoP proof；401 时清除对应缓存并重试一次。
- 新增 Console Image/Image Edit/Video 调用及 `grok-imagine-*-console` 模型路由，支持账号池和请求直传 SSO；视频采用异步创建/轮询，可信下载仅允许 HTTPS `vidgen.x.ai` 及其子域。
- 为异步 `videoJob` 状态及全局任务表增加读写锁，消除后台轮询与查询并发访问风险。
- 验证完成：定向 DPoP/媒体单测、`internal/api` 全包和排除真实 Cloudflare E2E 的 `internal/grok` 均通过 `-race`；`go test -run '^$' ./...`、`go build ./...`、`go vet ./...`、`git diff --check` 通过。Windows `-race` 需使用本机 SEH/UCRT MinGW；默认旧版 GCC 8.1 `posix-sjlj` 会以 `0xc0000139` 启动失败。
- 真实 Cloudflare E2E 未作为代码验收依据：本机 `cf_clearance` 已过期，现有环境返回 403。
